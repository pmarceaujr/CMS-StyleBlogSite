const router = require('express').Router();
const withAuth = require('../../utils/auth');
const { Comments, Review } = require('../../models');

console.log("In company Routes")


module.exports = router;